# 项目检查与优化记录（2026-09-08）

## 范围与依据

检查了 React 页面与请求层、Go 认证/预约/会员/房间设备/通知/管理服务、SQLite 初始化、启动脚本及项目文档。
依据历史 OpenSpec 预约与前端规格，按 Karpathy 四项原则修复有证据的问题。
没有改变技术栈、页面设计、业务范围或老板例外调整权限。

## 已落地

| 问题 | 处理 |
| --- | --- |
| 没有统一项目开发规则 | 固定版本 skill，AGENTS.md/CLAUDE.md/OpenSpec 接入并记录来源 |
| 固定预约日期过期，测试缓存掩盖失败 | 预约服务内部测试时钟；统一检查强制无缓存执行 |
| 候选时段逐一查询房间冲突 | 批量读取当日占用区间，正常营业日共四次查询，候选在内存计算 |
| 维护房间仍返回空闲时段 | 查询房间状态，不可预约时返回空列表 |
| 时间偏移影响营业判断和老板日期筛选 | 统一按 UTC+8 校验对齐、营业时间与日期筛选 |
| SQLite 外键只在初始连接开启 | DSN 对每条连接启用外键、5 秒忙等待和 immediate 事务 |
| 同起点不同时长共用 key，切换条件残留选择 | 起止时间共同标识；切换日期/房间清空；取消过期请求；成功后刷新 |
| 认证表单与浏览器历史不同步，重置链接未填令牌 | 表单由当前路由决定，读取链接令牌，保留注册/重置成功提示 |
| 自定义请求头覆盖 JSON 头 | 使用 Headers 合并，并保留显式类型、凭据和取消信号 |
| 多处失败被吞掉，重复提交无保护 | 共用反馈 hook，老板操作、顾客取消/通知及退出显示结果并防重入 |
| 构建失败仍启动、文档路径失效 | 启动脚本失败即停、恢复环境；增加检查入口并修正文档 |

## 验证结果

- 初始 `go test ./...` 命中缓存；改为 `go test -count=1 ./...` 复现三个过期日期失败。
- 最终 `scripts/check.ps1` 通过：Go 格式检查、`go vet ./...`、全部无缓存 Go 测试、4 个 Node 用例及 Vite 生产构建。
- 新增后端回归覆盖维护房间、清场边界、UTC 与非整小时偏移、门店日期筛选、并发预约唯一成功、两条连接的外键违规拒绝。
- `openspec validate harden-project-foundations --type change --strict --no-interactive` 通过。
- 浏览器连接独立端口 18080、临时数据库，验证认证后退、重置令牌自动填入、顾客登录、不同时长独立选择、切换房间/日期清空选择、真实预约成功、老板登录及新增房间失败/成功提示。
- 脚本故障注入验证：前端构建失败后不运行 seed/server，目录与环境恢复；格式检查失败后不运行后续步骤。
- `git diff --check` 通过。

## 验证边界

这是针对已确认问题的一轮项目优化，不是性能压测或穷尽性安全审计。
时段查询次数减少已从实现确认，未宣称特定延迟提升比例。
浏览器测试覆盖本轮关键交互，未遍历全部设备/会员组合或发送真实邮件；邮件保持日志适配器。
可用时段属于读取快照，提交时以事务校验为准。现有预约通知仍在预约提交后创建，并非跨服务原子事务。

变更与验收清单已归档：`openspec/changes/archive/2026-09-08-harden-project-foundations/`。
主规格已同步至 `openspec/specs/project-reliability/spec.md`，5 项要求与归档增量一致，并通过严格校验。

## 2026-09-09 前端体验与房间演示场景

本轮变更：`openspec/changes/improve-frontend-experience/`，尚未归档。

| 问题 | 最终处理 |
| --- | --- |
| 一次展示全天多个时长，选项过多 | 先选 0.5–3 小时时长，再切换上午/下午/晚上；默认一小时开放日展示 8 个上午候选，其他候选仍可访问；切换清空旧选择 |
| 已完成状态被卡片纵向拉伸，历史时间截取接口字符串 | 徽标独立高度；历史时间转换为 UTC+8；历史倒序、当前正序；展示房间名称；每 30 秒及返回窗口刷新接口状态 |
| 只有已读通知时未读区域是空壳，分组变量遮蔽读取函数 | 各分组独立空态；修复点击标记已读；全空、全已读和正常读取流程分别验证 |
| 房间只有简单介绍 | A 房落日合奏室、B 房蓝调大排练室、C 房微光创作室，13 类随房设备配置；首页及预约页展示示意、用途和设备状态；首页选房带入预约 |
| 手机菜单不自动关闭 | React 状态控制、遮罩、Esc 和导航后关闭；隐藏菜单不参与键盘导航 |
| 认证导航和请求反馈 | 保留同角色站内目标；返回/前进时同步表单；启动服务失败显示重试；登录密码支持正确自动填充类型 |
| 会员方案只能新增默认月卡 | 补全方案类型、价格和次数录入，数字字段增加约束；场馆设置提交中禁止编辑，防止保存响应覆盖输入 |
| 后台操作后旧筛选覆盖当前列表 | 操作完成通过 revision 触发当前条件查询，取消过期请求；加载阶段不显示空记录 |

### 本轮验收

- 最终 `scripts/check.ps1` 通过：Go 格式、vet、所有无缓存 Go 测试、7 个 Node 测试、生产构建。
- `openspec validate improve-frontend-experience --type change --strict --no-interactive` 通过。
- 使用隔离数据库 `tmp/frontend-experience-v2.db` 与端口 18082，headless Edge / Playwright 验证：登录回原目标页、手机号自动填入、时长/时段切换、真实创建/取消预约、通知标记已读、首页选中 B 房进入预约、3 个房间与 13 个设备分区、老板新增 5 次方案。
- 使用接口测试夹具验证全空通知，以及 `completed` + UTC 时间预约的历史分组、10:00–11:00 北京时间、无取消入口、徽标高度小于 35px。
- 桌面 1440px 与手机 390px 截图检查；手机菜单导航后关闭，页面无横向溢出。
- seed 用例验证重复执行不重复添加固定设备，并保留老板自定义房间描述。
- 先前工具观察疑似“登录目标丢失、手机号为空”，本轮使用独立浏览器完整等待异步完成后，两项均通过；未继续声称仍有故障。

### 演示说明与边界

重新运行 `scripts/start-demo.ps1` 会构建前端并填充场景资料。旧 A/B/C 默认描述会升级，老板修改过的描述保持不变；正式数据不应运行演示 seed。
布局为数据驱动的平面分区示意，并非实景照片、精确房间图或交互 3D。公共设备仍在预约提交时验证库存。
预约完成状态以后台为准，浏览器不自行篡改状态；后台每分钟处理到期预约，页面刷新存在短暂间隔。
已完成显示使用接口夹具验证，未宣称对用户现有预约数据做过更改。未发送真实邮件，未对所有会员/设备组合做穷尽测试。

### 首页三间房展示调整

首页改用紧凑概览卡：容量、首句用途介绍和随房设备标签默认可见，完整介绍、布局与设备明细按需展开。三间房用不同强调色区分，预约按钮底部对齐，暂停房间显示“暂不可预约”。预约页仍可查看完整场景。
1440px 浏览器实测三张默认卡片高度均约 531px，按钮纵坐标一致；验证展开/收起、B 房带入预约、390px 手机展开无横向溢出。完整项目检查通过，浏览器未捕获脚本错误。

### 2026-09-10 立体布局与阶段提交

布局进一步升级为 SVG 等距立体示意：木地板、吸音墙板、鼓组/键盘/音箱模型与设备编号，编号对应下方清单；保留真实接口的数量与维护状态。无新增运行依赖，非可旋转漫游的 3D 引擎。
最终完整检查和 OpenSpec strict 通过；浏览器验证卡片等高、展开/收起、B 房带入预约与 390px 立体布局无溢出，并检查截图。按用户要求，本阶段包含此前尚未提交的前端修复和演示场景，统一整理提交并推送跟踪远端；之后阶段完成即提交推送。

### 2026-09-10 鼓组重复数据及设备详情修复

只读检查本地数据库确认 B 房存在 13 条名称、默认描述、数量与状态完全相同的旧 seed 架子鼓记录。新增迁移 004，仅删除该房间可用、数量为一、描述为“房间固定架子鼓”的重复记录，保留最早 ID；不删除定制、不同数量或维护状态记录。先用 SQLite backup 创建本地备份，再通过项目迁移入口应用，复核 B 房架子鼓剩一条。数据库及备份不提交。

鼓组按套计量，详情明确一套五鼓包含底鼓 1、军鼓 1、悬挂通鼓 2、落地通鼓 1，另含踩镲一对、吊镲、叮叮镲和支架鼓凳。立体示意将这些部件组成一套鼓组；单独的鼓凳、军鼓等名称不会再匹配为完整鼓组。模型以一项配置为单位，实际数量以清单为准。

迁移回归覆盖 13 条重复记录、定制/数量/维护例外、最早 ID 保留与重复执行；完整项目检查、8 个 Node 用例、OpenSpec strict 通过。首页展开、选房与 390px 布局完成浏览器回归。
